<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RGB4U_App" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <!-- 스타일 재정의 추가 -->
    <style name="Theme.Navigation.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:width">40dp</item> <!-- 원하는 너비로 설정 -->
        <item name="android:height">28dp</item> <!-- 원하는 높이로 설정 -->
        <item name="android:color">@color/blue2_10</item>
        <item name="android:textSize">16sp</item>
        <item name="android:fontFamily">@font/nanumsquareroundbold</item>
    </style>

    <style name="Theme.RGB4U_App" parent="Base.Theme.RGB4U_App" />

    <style name="CustomDatePickerStyle" parent="">
        <item name="colorControlNormal">@color/main</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColorHint">@color/gray2</item>
        <item name="android:textSize">16sp</item>
        <item name="android:fontFamily">@font/nanumsquareroundregular</item>
    </style>
</resources>