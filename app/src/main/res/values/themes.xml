<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RGB4U_App" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.RGB4U_App" parent="Base.Theme.RGB4U_App" />

    <style name="CustomDatePickerStyle" parent="">
        <item name="colorControlNormal">@color/main</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColorHint">@color/gray2</item>
        <item name="android:textSize">16sp</item>
    </style>
</resources>