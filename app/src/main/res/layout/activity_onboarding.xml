<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_light">

    <!-- Toolbar -->
    <include
        android:id="@+id/toolbar_base1"
        layout="@layout/toolbar_base1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <!-- Startline with lines and text -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingVertical="8dp">

        <!-- Left line -->
        <View
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1"
            android:layout_marginLeft="16dp"
            android:background="@color/gray2" />

        <!-- Text -->
        <TextView
            android:id="@+id/startline"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="모아님이 입장하셨습니다"
            android:textColor="@color/gray2"
            style="@style/Cp2_Regular"
            android:layout_marginHorizontal="8dp" />

        <!-- Right line -->
        <View
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1"
            android:layout_marginRight="16dp"
            android:background="@color/gray2" />
    </LinearLayout>

    <!-- RecyclerView for chat bubbles -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="8dp" />

    <!-- Button for interaction -->
    <android.widget.Button
        android:id="@+id/btnNext"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_marginRight="40dp"
        android:layout_marginLeft="40dp"
        android:layout_gravity="center"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="24dp"
        android:background="@drawable/rounded_button_main_line"
        android:stateListAnimator="@null"
        android:textColor="@color/black"
        style="@style/Bd2_Bold"
        android:text="다음" />
</LinearLayout>
