<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/>
    <stroke android:width="1.5dp" android:color="@color/main"/> <!-- 기본 테두리 색상 -->
    <corners android:radius="8dp"/>
</shape>
